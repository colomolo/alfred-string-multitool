{
  "name": "alfred-string-multitool",
  "version": "1.0.0",
  "description": "String Multitool for Alfred",
  "scripts": {
    "build": "esbuild src/*.js --bundle --platform=node --outdir=bundle && chmod +x bundle/*.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "slugify": "1.6.6"
  },
  "devDependencies": {
    "esbuild": "0.25.10"
  }
}
